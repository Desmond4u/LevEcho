# ETF mapping review

Generated at: `2026-09-05T12:38:20Z`

Approved active pairs in proposal: **30**

Pending records: **16**

## Source errors

- ProShares https://www.proshares.com/our-etfs/leveraged-and-inverse: DiscoverySourceError: unable to read issuer tables from https://www.proshares.com/our-etfs/leveraged-and-inverse: No tables found
- ProShares https://www.proshares.com/resources/data-downloads: DiscoverySourceError: unable to read issuer tables from https://www.proshares.com/resources/data-downloads: No tables found

## Pending changes

- `AAPD` — unsupported leverage; v1 supports signed 2x and 3x only
- `AMDD` — unsupported leverage; v1 supports signed 2x and 3x only
- `AMZD` — unsupported leverage; v1 supports signed 2x and 3x only
- `AVS` — unsupported leverage; v1 supports signed 2x and 3x only
- `CSCS` — unsupported leverage; v1 supports signed 2x and 3x only
- `GGLS` — unsupported leverage; v1 supports signed 2x and 3x only
- `METD` — unsupported leverage; v1 supports signed 2x and 3x only
- `MSFD` — unsupported leverage; v1 supports signed 2x and 3x only
- `MUD` — unsupported leverage; v1 supports signed 2x and 3x only
- `NFXS` — unsupported leverage; v1 supports signed 2x and 3x only
- `NVDD` — unsupported leverage; v1 supports signed 2x and 3x only
- `ORCS` — unsupported leverage; v1 supports signed 2x and 3x only
- `PALD` — unsupported leverage; v1 supports signed 2x and 3x only
- `PLTD` — unsupported leverage; v1 supports signed 2x and 3x only
- `QCMD` — unsupported leverage; v1 supports signed 2x and 3x only
- `TSLS` — unsupported leverage; v1 supports signed 2x and 3x only

Review issuer references and leverage before applying any pending mapping to `data/approved_pairs.json`.
